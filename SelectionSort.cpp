#include <iostream>
#include "utils.cpp"

template <typename T>
void selection_sort(T* array_ptr, unsigned int array_len)
{
	T min_val{ array_ptr[0] };
	for (unsigned int idx = 0; idx < array_len; idx++)
	{
	
	}
}
